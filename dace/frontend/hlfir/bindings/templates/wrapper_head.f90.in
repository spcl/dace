  subroutine {entry}_dace({outer_dummy_list})
{outer_dummy_decls}

    ! Flat pointers aliased onto struct members (c_loc / c_f_pointer).
{flat_ptr_decls}

    ! Scratch arrays for deep-copy members (complex-split etc.).
{scratch_decls}

    ! Local scalars for SDFG free-symbol values.
{symbol_decls}

    integer(c_int) :: dace_err
