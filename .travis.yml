dist: xenial
language: python
python:
  - "3.6"
  - "3.7"  

# Dependencies
before_install:
  - sudo apt-get install libyaml-dev cmake

install:
  - pip install .

script: NOSTATUSBAR=1 ./test_all.sh
